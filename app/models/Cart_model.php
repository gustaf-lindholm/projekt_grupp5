<?php

/**
* 
*/
class Cart_model extends Base_model
{
	
	public function cartItems()
	{
		// get the array with chosen product(s) from session
		// show product(s) from session
		// alter product(s)
		// save final product list to session
	}
}